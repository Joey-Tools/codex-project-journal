interface:
  display_name: "Project Journal"
  short_description: "Maintain adopted journals and durable workstream state."
  default_prompt: "Use $project-journal to update the smallest applicable journal layer when this repo has adopted or requires the workflow, or the task has an explicit durable-state need."
